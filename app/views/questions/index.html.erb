<h3>All Questions</h3>

<% @questions.each do |question| %>
<div>
    <h2><%= link_to question.title, question_path(question) %> | auther: <%= question.user_id %></h2>
    <p><%= question.body %></p>
    
    <hr>
</div>
<% end %>